
/**
 * Utility function to log navigation events with consistent formatting
 */
export const logNavigationEvent = (event: string, details: any) => {
  console.log(`[Navigation Event] ${event}:`, details);
};
